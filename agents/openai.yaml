interface:
  display_name: "Brainerd"
  short_description: "Route repo-local brain work to the right harness-specific Brainerd surface"
  default_prompt: "Use $brainerd as the umbrella router, then pick the correct harness-specific surface: $pi-init/$pi-reflect/$pi-ruminate, $codex-init/$codex-reflect/$codex-ruminate, or $claude-init/$claude-reflect/$claude-ruminate."
