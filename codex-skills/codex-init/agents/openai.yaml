interface:
  display_name: "Codex Init"
  short_description: "Initialize a repo brain for Codex"
  default_prompt: "Use $codex-init to create the repo-local brain, install the managed AGENTS.md block, and preview or apply operational bootstrap."
